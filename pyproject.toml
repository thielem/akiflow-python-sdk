[project]
name = "akiflow"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.27",
]

[project.optional-dependencies]
docs = ["pdoc>=14"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
